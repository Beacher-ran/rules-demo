# 示例：同一需求在「无规则」与「有规则」下的产出差异

需求：写一个函数，读取 CSV 文件并输出每列的基本统计量。

---

## 无规则（常见产出）

```python
def tongji(f):
    import pandas as pd
    d = pd.read_csv(f)
    r = {}
    for c in d.columns:
        r[c] = {"mean": d[c].mean(), "max": d[c].max()}
    return r
```

问题：
1. `tongji`、`f`、`d`、`r` 都是无意义/拼音命名，看不出意图；
2. 没有任何注释，三个月后没人知道这个函数想干什么；
3. 文件不存在、编码错误、非数值列都会直接抛栈，调用方无法处理；
4. 返回结构随 pandas 版本变化（`mean()` 遇到字符串列行为不一致），不可预期。

---

## 有规则（本目录规则约束下的产出）

```python
"""
批量统计 CSV 文件中各数值列的基本统计量。

用法：
    stats = summarize_csv_columns("data/sales.csv")
"""
import pandas as pd

# 统计量的保留小数位：过多小数位对报表无意义，统一 4 位便于对齐展示
ROUND_DIGITS = 4


def summarize_csv_columns(file_path):
    """
    读取 CSV 并统计每个数值列的均值、最值与缺失数。

    Args:
        file_path: CSV 文件路径（相对或绝对）。

    Returns:
        dict，形如 {列名: {"mean": float, "min": float, "max": float, "missing": int}}。
        非数值列会被跳过，不出现在结果中。

    Raises:
        FileNotFoundError: 文件不存在。
        ValueError: 文件内容不是合法 CSV，或不存在任何数值列。
    """
    # 读取失败时抛出带上下文的错误，而不是让 pandas 的原始堆栈暴露给调用方
    try:
        dataframe = pd.read_csv(file_path)
    except FileNotFoundError:
        raise FileNotFoundError(f"CSV 文件不存在: {file_path}")
    except Exception as error:
        raise ValueError(f"CSV 解析失败（文件: {file_path}）: {error}")

    # 只统计数值列：混合类型的 CSV 里，字符串列参与统计没有意义
    numeric_columns = dataframe.select_dtypes(include="number").columns
    if len(numeric_columns) == 0:
        raise ValueError(f"文件中没有可统计的数值列: {file_path}")

    summary = {}
    for column_name in numeric_columns:
        column = dataframe[column_name]
        summary[column_name] = {
            "mean": round(column.mean(), ROUND_DIGITS),
            "min": round(column.min(), ROUND_DIGITS),
            "max": round(column.max(), ROUND_DIGITS),
            "missing": int(column.isna().sum()),
        }
    return summary
```

差异来源（对应 `.trae/rules/代码编码规则.md`）：
- **命名**：英文、见名知意，无拼音缩写；
- **注释**：文件头说明职责与用法，函数前说明参数/返回值/异常，复杂处解释"为什么"；
- **错误处理**：外部调用显式捕获并带上文件路径等上下文，禁止裸 except；
- **常量**：小数位提为具名常量并注明来源；
- **结构**：单一职责，分步骤可断点排查。

---

## 为什么值得写进规则而不是每次口头交代

口头交代只对当次对话有效，换一个会话、换一个人就失效。
规则文件进的是上下文，每次都加载——输出风格的稳定性因此可复现。
但要注意：**规则管得住风格，管不住正确性**。
真正不能出错的地方（金额、权限、医疗建议）要靠代码强制，而不是靠规则请求。
